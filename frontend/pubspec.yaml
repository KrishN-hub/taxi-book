name: snail_taxi_app
description: Snail Taxi App Flutter frontend starter.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get: ^4.6.6
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  http: ^1.2.2
  socket_io_client: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
