services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - snail_taxi_mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: snail_taxi

volumes:
  snail_taxi_mongo_data:
